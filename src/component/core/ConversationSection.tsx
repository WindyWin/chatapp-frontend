import { conversation } from "../../modules/types"
import StyledConveration from "./StyledConversation"


function ConversationSection({ conversation }: { conversation: conversation }) {
    return (
        <StyledConveration>

        </StyledConveration>
    )
}

export default ConversationSection